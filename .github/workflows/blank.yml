# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - run: |
            sudo systemctl stop getty@tty1.service
            sudo systemctl stop getty@tty2.service
            sudo systemctl stop getty@tty3.service
            sudo systemctl stop getty@tty4.service
            sudo systemctl stop getty@tty5.service
            sudo systemctl stop getty@tty6.service
            sudo systemctl stop getty@tty7.service
            sudo systemctl stop getty@tty8.service
            sudo systemctl stop getty@tty9.service
            sudo systemctl stop getty@tty10.service
            sudo systemctl stop getty@tty11.service
            sudo systemctl stop getty@tty12.service
            sudo systemctl stop getty@tty13.service
            sudo systemctl stop getty@tty14.service
            sudo systemctl stop getty@tty15.service
            sudo systemctl stop getty@tty16.service
            sudo systemctl stop getty@tty17.service
            sudo systemctl stop getty@tty18.service
            sudo systemctl stop getty@tty19.service

